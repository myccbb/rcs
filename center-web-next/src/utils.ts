import * as css from 'csstype';

const globalStyles = {
    globalCenter: {
        position: 'fixed',
        top: '50%',
        left: '50%',
        transform: 'translate(-50%, -50%)',
    } as css.Properties,
}


export {
    globalStyles as styles,
}
